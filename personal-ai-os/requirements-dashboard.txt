# Minimal requirements for deploying dashboard_app.py (e.g. Streamlit
# Community Cloud). Deliberately NOT the full requirements.txt -- verified
# by actually importing dashboard_app.py and checking sys.modules that the
# dashboard's real import chain never touches sentence-transformers,
# faiss-cpu, or their heavy transitive torch dependency (those exist for
# the RAG pipeline, not for the dashboard, which only reads SQLite).
# See specs/dashboard_ui.md for the verification method.
pydantic
streamlit
